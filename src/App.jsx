import React,{useEffect,useMemo,useState} from 'react';
import {BrowserRouter,Link,NavLink,Navigate,Outlet,Route,Routes,useLocation,useNavigate,useOutletContext} from 'react-router-dom';
import {Activity,Award,Bell,CalendarDays,Check,ChevronRight,Clipboard,GitCompareArrows,GraduationCap,Heart,LogOut,Mail,MapPin,MessageCircle,Plus,Radar as RadarIcon,Save,Send,Settings as SettingsIcon,Sparkles,Target,Trash2,Users,X} from 'lucide-react';
import {PolarAngleAxis,PolarGrid,PolarRadiusAxis,Radar,RadarChart,ResponsiveContainer,Tooltip} from 'recharts';
import {AuthProvider,useAuth} from './context/AuthContext';
import ProtectedRoute from './components/ProtectedRoute';
import {supabase} from './lib/supabase';

const tiers={1:'Международный / национальный уровень',2:'Региональный лидер или заметный результат',3:'Стабильная роль в школе / городе',4:'Разовая или начальная школьная активность'};
const countries=['USA','UK','Netherlands','Hong Kong','Kazakhstan','Switzerland'];
const draftKey='northstar-profile-draft-v2';
const emptyProfile={full_name:'',school:'',city:'',country:'Kazakhstan',grade:11,target_major:'Computer Science',gpa_unweighted:3.7,gpa_weighted:null,gpa_scale:4,english_exam_type:'IELTS',english_score:6.5,sat:null,act:null,ent_unt:null,nuet:null,interview_ready:false,annual_budget:20000,currency:'USD',needs_full_aid:false,portfolio_public:false,target_countries:['USA','UK'],narrative_about_me:''};

function Landing(){return <div className="min-h-screen bg-[#f8f6f1]"><header className="max-w-6xl mx-auto px-5 py-5 flex justify-between items-center"><Link to="/" className="flex items-center gap-2 font-extrabold"><span className="logo-mark">✦</span>northstar</Link><Link className="btn-ghost" to="/login">Войти</Link></header><main className="max-w-6xl mx-auto px-5 py-10"><section className="grid md:grid-cols-[1.1fr_.9fr] gap-10 items-center min-h-[72vh]"><div><div className="eyebrow"><span className="dot"/>профиль · оценка · маршрут</div><h1 className="display text-6xl md:text-8xl leading-[.92] mt-6">Твой следующий шаг уже <em>виден.</em></h1><p className="text-lg muted max-w-xl mt-7 leading-relaxed">Northstar собирает академический профиль, оценивает связность Major / Spike и превращает поступление в календарь конкретных действий.</p><Link className="btn-primary mt-8" to="/login">Собрать мой маршрут <ChevronRight size={18}/></Link></div><div className="hero-card"><div className="compass">✦</div><div className="mt-8 text-white"><div className="text-xs uppercase tracking-[.2em] text-[#c7d8c3]">в одном кабинете</div><h2 className="display text-4xl mt-2">Портфолио.<br/>Шансы. Дедлайны.</h2><p className="text-[#c7d8c3] mt-3">С данными в Postgres и персональной AI-оценкой.</p></div></div></section></main></div>}

function Login(){const {user,configured,signIn,signUp,resetPassword}=useAuth();const navigate=useNavigate();const location=useLocation();const [mode,setMode]=useState('login');const [form,setForm]=useState({name:'',email:'',password:''});const [message,setMessage]=useState('');const [busy,setBusy]=useState(false);if(user)return <Navigate to={location.state?.from||'/portfolio'} replace/>;async function submit(e){e.preventDefault();setBusy(true);setMessage('');let result;if(mode==='forgot')result=await resetPassword(form.email);else if(mode==='login')result=await signIn(form.email,form.password);else result=await signUp(form.email,form.password,form.name);setBusy(false);if(result.error)setMessage(result.error.message);else if(mode==='login')navigate(location.state?.from||'/portfolio');else if(mode==='forgot')setMessage('Проверь почту — мы отправили ссылку для нового пароля.');else setMessage('Проверь почту и подтверди регистрацию.')}const title=mode==='login'?'С возвращением.':mode==='signup'?'Создай свой маршрут.':'Восстановить доступ.';return <div className="min-h-screen bg-[#17352e] grid place-items-center p-5"><form onSubmit={submit} className="panel p-7 w-full max-w-md"><Link to="/" className="flex items-center gap-2 font-extrabold"><span className="logo-mark">✦</span>northstar</Link><h1 className="display text-4xl mt-7">{title}</h1><p className="muted mt-2">{mode==='forgot'?'Укажи email — Supabase пришлёт безопасную ссылку.':'Профиль и прогресс будут храниться в защищённой базе.'}</p>{mode==='signup'&&<Field label="Имя"><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></Field>}<Field label="Email"><input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></Field>{mode!=='forgot'&&<Field label="Пароль"><input required minLength="8" type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></Field>}{!configured&&<p className="error mt-4">Добавь VITE_SUPABASE_URL и VITE_SUPABASE_ANON_KEY.</p>}{message&&<p className={message.startsWith('Проверь')?'notice mt-4':'error mt-4'}>{message}</p>}<button disabled={busy||!configured} className="btn-primary w-full justify-center mt-6">{busy?'Подключаем…':mode==='login'?'Войти':mode==='signup'?'Зарегистрироваться':'Отправить ссылку'}</button>{mode==='login'&&<button type="button" className="btn-ghost w-full justify-center mt-3" onClick={()=>{setMode('forgot');setMessage('')}}>Забыли пароль?</button>}<button type="button" className="btn-ghost w-full justify-center mt-3" onClick={()=>{setMode(mode==='login'?'signup':'login');setMessage('')}}>{mode==='login'?'Нет аккаунта? Создать':'Вернуться ко входу'}</button></form></div>}

function ResetPassword(){const {updatePassword}=useAuth();const navigate=useNavigate();const [password,setPassword]=useState('');const [confirm,setConfirm]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e){e.preventDefault();if(password.length<8){setError('Пароль должен содержать минимум 8 символов.');return}if(password!==confirm){setError('Пароли не совпадают.');return}setBusy(true);setError('');const {error:updateError}=await updatePassword(password);setBusy(false);if(updateError)setError(updateError.message);else navigate('/portfolio',{replace:true})}return <div className="min-h-screen bg-[#17352e] grid place-items-center p-5"><form onSubmit={submit} className="panel p-7 w-full max-w-md"><Link to="/" className="flex items-center gap-2 font-extrabold"><span className="logo-mark">✦</span>northstar</Link><h1 className="display text-4xl mt-7">Новый пароль.</h1><p className="muted mt-2">Ссылка из письма создаёт временную защищённую сессию.</p><Field label="Новый пароль"><input required minLength="8" type="password" value={password} onChange={e=>setPassword(e.target.value)}/></Field><Field label="Повтори пароль"><input required minLength="8" type="password" value={confirm} onChange={e=>setConfirm(e.target.value)}/></Field>{error&&<p className="error mt-4">{error}</p>}<button disabled={busy} className="btn-primary w-full justify-center mt-6">{busy?'Сохраняем…':'Сохранить пароль'}</button></form></div>}

function Shell(){const {user,signOut}=useAuth();const [profile,setProfile]=useState(null);const [universities,setUniversities]=useState([]);const [loading,setLoading]=useState(true);useEffect(()=>{(async()=>{const [{data:p},{data:u}]=await Promise.all([supabase.from('profiles').select('*').eq('user_id',user.id).single(),supabase.from('universities').select('*').order('qs_rank')]);setProfile(p);setUniversities(u||[]);setLoading(false)})()},[user.id]);if(loading)return <div className="app-loader"><Sparkles className="twinkle"/><p>Загружаем профиль…</p></div>;return <div className="min-h-screen bg-[#f8f6f1]"><header className="app-header"><Link to="/" className="flex items-center gap-2 font-extrabold"><span className="logo-mark">✦</span>northstar</Link><nav className="app-nav"><NavLink to="/portfolio">Портфолио</NavLink><NavLink to="/evaluator">AI-оценка</NavLink><NavLink to="/assistant">AI-ассистент</NavLink><NavLink to="/universities">Вузы</NavLink><NavLink to="/compare">Сравнение</NavLink><NavLink to="/roadmap">Roadmap</NavLink><NavLink to="/networking">Письма</NavLink><NavLink to="/settings">Настройки</NavLink></nav><button className="icon-button" aria-label="Выйти" onClick={signOut}><LogOut size={18}/></button></header><DeadlineNotifications user={user} profile={profile}/><main className="max-w-6xl mx-auto px-5 py-8"><Outlet context={{profile,setProfile,universities}}/></main></div>}
const useWorkspace=()=>useOutletContext();

function DeadlineNotifications({user,profile}){const [items,setItems]=useState([]);useEffect(()=>{if(!profile?.id)return;(async()=>{const today=new Date();today.setHours(0,0,0,0);const end=new Date(today);end.setDate(end.getDate()+7);const {data:events}=await supabase.from('calendar_events').select('id,title,date').eq('profile_id',profile.id).eq('type','deadline').gte('date',today.toISOString().slice(0,10)).lte('date',end.toISOString().slice(0,10));const rows=(events||[]).map(event=>{const eventDate=new Date(`${event.date}T00:00:00`);const days=Math.max(0,Math.round((eventDate-today)/86400000));const reminder_days=days<=1?1:days<=3?3:7;return {user_id:user.id,profile_id:profile.id,event_id:event.id,reminder_days,channel:'in_app'}});if(rows.length)await supabase.from('deadline_notifications').upsert(rows,{onConflict:'event_id,reminder_days,channel',ignoreDuplicates:true});const {data}=await supabase.from('deadline_notifications').select('id,reminder_days,calendar_events(title,date)').eq('profile_id',profile.id).is('shown_at',null).order('created_at').limit(3);setItems(data||[])})()},[profile?.id,user.id]);async function dismiss(id){setItems(current=>current.filter(item=>item.id!==id));await supabase.from('deadline_notifications').update({shown_at:new Date().toISOString()}).eq('id',id)}if(!items.length)return null;return <div className="max-w-6xl mx-auto px-5 pt-4 space-y-2">{items.map(item=><div className="notice flex items-center gap-3" key={item.id}><Bell size={18}/><div className="flex-1"><b>Дедлайн через {item.reminder_days===1?'1 день':`${item.reminder_days} дня`}:</b> {item.calendar_events?.title}<small className="demo">{item.calendar_events?.date}</small></div><button className="icon-button" aria-label="Скрыть уведомление" onClick={()=>dismiss(item.id)}><X size={16}/></button></div>)}</div>}

function Portfolio(){const {profile,setProfile}=useWorkspace();const [draft,setDraft]=useState(()=>{try{return {...emptyProfile,...profile,...JSON.parse(localStorage.getItem(draftKey)||'{}')}}catch{return {...emptyProfile,...profile}}});const [step,setStep]=useState(0);const [activities,setActivities]=useState([]);const [olympiads,setOlympiads]=useState([]);const [saving,setSaving]=useState(false);const [message,setMessage]=useState('');useEffect(()=>{localStorage.setItem(draftKey,JSON.stringify(draft))},[draft]);useEffect(()=>{Promise.all([supabase.from('activities').select('*').eq('profile_id',profile.id),supabase.from('olympiads').select('*').eq('profile_id',profile.id)]).then(([a,o])=>{setActivities(a.data||[]);setOlympiads(o.data||[])})},[profile.id]);const set=(key,value)=>setDraft(x=>({...x,[key]:value}));const toggleCountry=c=>set('target_countries',draft.target_countries.includes(c)?draft.target_countries.filter(x=>x!==c):[...draft.target_countries,c]);async function save(){setSaving(true);const payload={...draft,user_id:profile.user_id,id:profile.id};delete payload.created_at;delete payload.updated_at;delete payload.is_demo;delete payload.leaderboard_score;const {data,error}=await supabase.from('profiles').upsert(payload).select().single();if(!error){setProfile(data);localStorage.removeItem(draftKey);setMessage('Профиль сохранён в Postgres.')}else setMessage(error.message);setSaving(false)}async function addActivity(){const {data}=await supabase.from('activities').insert({profile_id:profile.id,name:'Новая активность',tier:'4',hours_per_week:2,description:''}).select().single();if(data)setActivities([...activities,data])}async function updateActivity(id,patch){setActivities(xs=>xs.map(x=>x.id===id?{...x,...patch}:x));await supabase.from('activities').update(patch).eq('id',id)}async function removeActivity(id){setActivities(xs=>xs.filter(x=>x.id!==id));await supabase.from('activities').delete().eq('id',id)}async function addOlympiad(){const {data}=await supabase.from('olympiads').insert({profile_id:profile.id,name:'Олимпиада',level:'school',result:'participant'}).select().single();if(data)setOlympiads([...olympiads,data])}const hk=draft.target_countries.includes('Hong Kong'),kz=draft.target_countries.includes('Kazakhstan');return <section><PageTitle icon={<GraduationCap/>} eyebrow="защищённый профиль" title="Портфолио, которое складывается в историю." sub="Данные сохраняются в Postgres; локально остаётся только черновик до нажатия «Сохранить»."/><div className="flex gap-2 mt-7 overflow-x-auto pb-2">{['Контекст','Major / Spike','Тесты','Активности','История'].map((x,i)=><button className={'step-pill '+(step===i?'active':'')} onClick={()=>setStep(i)} key={x}>{i+1}. {x}</button>)}</div><div className="panel p-6 md:p-8 mt-3">{step===0&&<div className="grid md:grid-cols-2 gap-5"><Field label="Имя"><input value={draft.full_name} onChange={e=>set('full_name',e.target.value)}/></Field><Field label="Школа"><input value={draft.school} onChange={e=>set('school',e.target.value)}/></Field><Field label="Город"><input value={draft.city} onChange={e=>set('city',e.target.value)}/></Field><Field label="Класс"><select value={draft.grade||11} onChange={e=>set('grade',Number(e.target.value))}><option value="9">9</option><option value="10">10</option><option value="11">11</option><option value="12">12</option></select></Field></div>}{step===1&&<><Field label="Major / Spike"><input value={draft.target_major} onChange={e=>set('target_major',e.target.value)} placeholder="Computer Science / AI safety / Economics"/></Field><Field label="О себе"><textarea rows="5" value={draft.narrative_about_me} onChange={e=>set('narrative_about_me',e.target.value)} placeholder="Какая тема связывает твои проекты, предметы и цели?"/></Field><Field label="Целевые страны"><div className="flex flex-wrap gap-2">{countries.map(c=><button type="button" key={c} className={'choice '+(draft.target_countries.includes(c)?'chosen':'')} onClick={()=>toggleCountry(c)}>{draft.target_countries.includes(c)&&<Check size={15}/>} {c}</button>)}</div></Field></>}{step===2&&<div className="grid md:grid-cols-2 gap-5"><Field label="GPA unweighted"><input type="number" step=".01" value={draft.gpa_unweighted||''} onChange={e=>set('gpa_unweighted',Number(e.target.value))}/></Field><Field label="Шкала GPA"><input type="number" value={draft.gpa_scale||4} onChange={e=>set('gpa_scale',Number(e.target.value))}/></Field><Field label="IELTS / TOEFL"><input type="number" step=".5" value={draft.english_score||''} onChange={e=>set('english_score',Number(e.target.value))}/></Field>{!kz&&<Field label="SAT"><input type="number" value={draft.sat||''} onChange={e=>set('sat',Number(e.target.value))}/></Field>}{kz&&<Field label="NUET — приоритет для Казахстана"><input type="number" value={draft.nuet||''} onChange={e=>set('nuet',Number(e.target.value))}/></Field>}{kz&&<Field label="ЕНТ / UNT"><input type="number" value={draft.ent_unt||''} onChange={e=>set('ent_unt',Number(e.target.value))}/></Field>}{hk&&<label className="check-row mt-6"><input type="checkbox" checked={draft.interview_ready} onChange={e=>set('interview_ready',e.target.checked)}/><span><b>Готовность к интервью</b><small className="demo">Для Гонконга это влияет на оценку</small></span></label>}</div>}{step===3&&<><div className="flex justify-between items-center"><div><h3 className="font-extrabold text-xl">Активности</h3><p className="muted text-sm">Tier помогает оценить масштаб, часы — глубину участия.</p></div><button className="btn-ghost" onClick={addActivity}><Plus size={16}/>Добавить</button></div><div className="space-y-4 mt-5">{activities.map(a=><div className="activity-editor" key={a.id}><input value={a.name} onChange={e=>updateActivity(a.id,{name:e.target.value})}/><select value={a.tier} onChange={e=>updateActivity(a.id,{tier:e.target.value})}>{Object.entries(tiers).map(([k,v])=><option value={k} key={k}>Tier {k} — {v}</option>)}</select><input type="number" min="0" max="168" value={a.hours_per_week} onChange={e=>updateActivity(a.id,{hours_per_week:Number(e.target.value)})} aria-label="Часов в неделю"/><textarea rows="2" value={a.description} onChange={e=>updateActivity(a.id,{description:e.target.value})}/><button className="icon-button" aria-label="Удалить активность" onClick={()=>removeActivity(a.id)}><Trash2 size={16}/></button></div>)}</div></>}{step===4&&<><div className="flex justify-between"><h3 className="font-extrabold text-xl">Олимпиады и награды</h3><button className="btn-ghost" onClick={addOlympiad}><Plus size={16}/>Добавить</button></div>{olympiads.map(o=><div key={o.id} className="compact-row"><Award size={17}/><input defaultValue={o.name} onBlur={e=>supabase.from('olympiads').update({name:e.target.value}).eq('id',o.id)}/><select defaultValue={o.level} onChange={e=>supabase.from('olympiads').update({level:e.target.value}).eq('id',o.id)}><option value="school">Школьный</option><option value="regional">Региональный</option><option value="national">Национальный</option><option value="international">Международный</option></select><input defaultValue={o.result} onBlur={e=>supabase.from('olympiads').update({result:e.target.value}).eq('id',o.id)}/></div>)}</>}</div><div className="flex justify-between items-center mt-6"><span className="source">{message}</span><button onClick={save} disabled={saving} className="btn-primary"><Save size={16}/>{saving?'Сохраняем…':'Сохранить профиль'}</button></div></section>}

function Universities(){const {profile,universities}=useWorkspace();const [leaders,setLeaders]=useState([]);const [leaderCount,setLeaderCount]=useState(0);const [shortlist,setShortlist]=useState(new Set());const [shortlistError,setShortlistError]=useState('');useEffect(()=>{Promise.all([supabase.rpc('get_public_leaderboard',{result_limit:10}),supabase.from('shortlist').select('university_id').eq('profile_id',profile.id)]).then(([leaderResult,shortlistResult])=>{setLeaders(leaderResult.data||[]);setLeaderCount(Number(leaderResult.data?.[0]?.total_count||0));setShortlist(new Set((shortlistResult.data||[]).map(x=>x.university_id)));if(shortlistResult.error)setShortlistError('Не удалось загрузить шортлист.')})},[profile.id]);async function toggleShortlist(universityId){setShortlistError('');if(shortlist.has(universityId)){const {error}=await supabase.from('shortlist').delete().eq('profile_id',profile.id).eq('university_id',universityId);if(error){setShortlistError(error.message);return}setShortlist(current=>{const next=new Set(current);next.delete(universityId);return next})}else{const {error}=await supabase.from('shortlist').insert({profile_id:profile.id,university_id:universityId});if(error){setShortlistError(error.message);return}setShortlist(current=>new Set([...current,universityId]))}}return <section><PageTitle icon={<MapPin/>} eyebrow="университеты и benchmark" title="Рейтинг — контекст, не решение." sub="QS указан для быстрой навигации; требования и acceptance rate зависят от программы и года."/><div className="flex flex-wrap items-center justify-between gap-3 mt-7"><span className="source">В шортлисте: {shortlist.size}</span><Link className="btn-ghost" to="/compare"><GitCompareArrows size={16}/>Сравнить выбранные</Link></div>{shortlistError&&<p className="error mt-3">{shortlistError}</p>}<div className="grid md:grid-cols-3 gap-4 mt-4">{universities.map(u=>{const selected=shortlist.has(u.id);return <article className="program-card" key={u.id}><div className="flex justify-between"><span className="badge bg-[#f2e6df]">QS #{u.qs_rank}</span><span className="source">{u.admission_type}</span></div><h3 className="font-extrabold text-lg mt-4">{u.name}</h3><p className="muted text-sm mt-1">{u.city}, {u.country}</p><div className="mt-4 text-sm">Acceptance: <b>{u.acceptance_rate}%</b> · GPA: <b>{u.gpa_requirement}</b></div><small className="demo">QS 2026; остальные показатели ориентировочные</small><button className={selected?'btn-primary w-full justify-center mt-4':'btn-ghost w-full justify-center mt-4'} onClick={()=>toggleShortlist(u.id)}><Heart size={16} fill={selected?'currentColor':'none'}/>{selected?'В шортлисте ✓':'В шортлист'}</button></article>})}</div><div className="panel p-6 mt-8"><div className="flex items-center gap-2"><Users size={18}/><h3 className="font-extrabold">Лидерборд · {leaderCount} открытых профилей</h3></div><div className="overflow-x-auto mt-4"><table className="w-full min-w-[620px]"><thead><tr><th>#</th><th>Профиль</th><th>Школа</th><th>Major / Spike</th><th>Score</th></tr></thead><tbody>{leaders.map((x,i)=><tr key={`${x.full_name}-${x.school}-${i}`}><td>{i+1}</td><td>{x.full_name}<small className="demo">{x.country}</small></td><td>{x.school}</td><td>{x.target_major}</td><td><b>{x.leaderboard_score}</b></td></tr>)}</tbody></table></div></div></section>}

function Compare(){const {profile}=useWorkspace();const [items,setItems]=useState([]);const [total,setTotal]=useState(0);const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{(async()=>{const {data:rows,count,error:shortlistError}=await supabase.from('shortlist').select('university_id,created_at,universities(*)',{count:'exact'}).eq('profile_id',profile.id).order('created_at').limit(4);if(shortlistError){setError(shortlistError.message);setLoading(false);return}const universityIds=(rows||[]).map(x=>x.university_id);let latest={};if(universityIds.length){const {data:evaluations,error:evaluationError}=await supabase.from('ai_evaluations').select('university_id,holistic_score,created_at').eq('profile_id',profile.id).in('university_id',universityIds).order('created_at',{ascending:false});if(evaluationError)setError(evaluationError.message);for(const evaluation of evaluations||[]){if(!latest[evaluation.university_id])latest[evaluation.university_id]=evaluation}}setItems((rows||[]).map(row=>({...row.universities,holistic_score:latest[row.university_id]?.holistic_score})));setTotal(count||0);setLoading(false)})()},[profile.id]);const requirement=(u,key)=>{const value=u.other_requirements?.[key];return Array.isArray(value)?value.join(', '):value||'—'};if(loading)return <div className="app-loader"><Sparkles className="twinkle"/><p>Собираем сравнение…</p></div>;return <section><PageTitle icon={<GitCompareArrows/>} eyebrow="сравнение шортлиста" title="Смотри на различия, а не на логотипы." sub="Одновременно сравниваются первые четыре вуза из твоего шортлиста и последняя сохранённая AI-оценка."/>{error&&<p className="error mt-5">{error}</p>}{total>4&&<div className="notice mt-5">В шортлисте {total} вузов. Здесь показаны первые 4 — убери лишние на странице вузов, чтобы изменить набор.</div>}{!items.length?<div className="empty-state panel mt-8"><Heart size={38}/><h3 className="font-extrabold mt-3">Шортлист пока пуст</h3><p className="muted">Добавь от 2 до 4 вузов, чтобы сравнить требования и оценки.</p><Link className="btn-primary mt-5" to="/universities">Выбрать вузы</Link></div>:<div className="overflow-x-auto panel mt-8 max-w-full"><table className="w-full min-w-[760px]"><thead><tr><th>Параметр</th>{items.map(u=><th key={u.id}>{u.name}</th>)}</tr></thead><tbody><tr><td>QS rank</td>{items.map(u=><td key={u.id}>#{u.qs_rank??'—'}</td>)}</tr><tr><td>Admission type</td>{items.map(u=><td key={u.id}>{u.admission_type}</td>)}</tr><tr><td>Acceptance rate</td>{items.map(u=><td key={u.id}>{u.acceptance_rate!=null?`${u.acceptance_rate}%`:'—'}</td>)}</tr><tr><td>GPA requirement</td>{items.map(u=><td key={u.id}>{u.gpa_requirement??'—'}</td>)}</tr><tr><td>Tests</td>{items.map(u=><td key={u.id}>{requirement(u,'tests')}</td>)}</tr><tr><td>Financial aid</td>{items.map(u=><td key={u.id}>{requirement(u,'aid')}</td>)}</tr><tr><td>Data note</td>{items.map(u=><td key={u.id}>{requirement(u,'data_note')}</td>)}</tr><tr><td>AI holistic score</td>{items.map(u=><td key={u.id}>{u.holistic_score!=null?<b>{u.holistic_score}/100</b>:<span>— <Link className="underline" to="/evaluator">Запусти AI-оценку</Link></span>}</td>)}</tr></tbody></table></div>}{items.length===1&&<p className="notice mt-5">Добавь ещё минимум один вуз для полезного сравнения.</p>}</section>}

function Evaluator(){const {profile,universities}=useWorkspace();const [universityId,setUniversityId]=useState('');const [evaluation,setEvaluation]=useState(null);const [loading,setLoading]=useState(false);const [phase,setPhase]=useState(0);const phases=['Анализируем профиль…','Проверяем Major / Spike…','Сравниваем с базой поступивших…','Считаем ориентировочные шансы…'];async function evaluate(){if(!universityId)return;setLoading(true);setEvaluation(null);setPhase(0);const timer=setInterval(()=>setPhase(x=>Math.min(x+1,phases.length-1)),900);const {data,error}=await supabase.functions.invoke('evaluate-profile',{body:{universityId}});clearInterval(timer);setLoading(false);if(error){let message=error.message;const status=error.context?.status;if(status===429){message='Дневной лимит ИИ-оценок исчерпан, попробуйте завтра.';try{const payload=await error.context.json();message=payload.error||message}catch{/* keep clear fallback */}}setEvaluation({error:message,rateLimited:status===429})}else setEvaluation(data)}const chart=useMemo(()=>{if(!evaluation||evaluation.error)return[];const avg=evaluation.university.average_admitted_profile||{};return [{axis:'Academics',student:evaluation.academics_score,admitted:avg.Academics||85},{axis:'Extracurriculars',student:evaluation.extracurriculars_score,admitted:avg.Extracurriculars||85},{axis:'Awards',student:evaluation.awards_score,admitted:avg.Awards||85},{axis:'Test Scores',student:evaluation.test_scores_score,admitted:avg['Test Scores']||85}]},[evaluation]);return <section><PageTitle icon={<RadarIcon/>} eyebrow="AI evaluator" title="Цифры плюс связность профиля." sub={`Оценка учитывает Major / Spike «${profile.target_major||'не задан'}», региональные требования и не является гарантией поступления.`}/><div className="panel p-6 mt-8 flex flex-col md:flex-row gap-4"><select value={universityId} onChange={e=>setUniversityId(e.target.value)}><option value="">Выбери университет</option>{universities.map(u=><option key={u.id} value={u.id}>QS #{u.qs_rank} · {u.name}</option>)}</select><button className="btn-primary whitespace-nowrap" disabled={!universityId||loading} onClick={evaluate}><Sparkles size={16}/>Запустить анализ</button></div>{loading&&<div className="panel p-7 mt-5"><div className="ai-skeleton"><Sparkles className="twinkle"/><div><b>{phases[phase]}</b><div className="skeleton-line mt-3"/><div className="skeleton-line short mt-2"/></div></div><div className="analysis-steps mt-6">{phases.map((x,i)=><span className={i<=phase?'done':''} key={x}>{i<phase?'✓':i+1} {x}</span>)}</div></div>}{evaluation?.error&&<div className="notice mt-5">{evaluation.error}{!evaluation.rateLimited&&' Проверь Edge Function и переменные окружения.'}</div>}{evaluation&&!evaluation.error&&<div className="grid lg:grid-cols-2 gap-5 mt-5"><div className="panel p-6"><div className="flex justify-between items-start"><div><div className="eyebrow">ориентировочная оценка</div><h3 className="display text-4xl mt-2">{evaluation.holistic_score}/100</h3></div><span className="badge bg-[#fff0d6]">не гарантия</span></div><div className="h-[330px] mt-4"><ResponsiveContainer><RadarChart data={chart}><PolarGrid/><PolarAngleAxis dataKey="axis" tick={{fontSize:11}}/><PolarRadiusAxis domain={[0,100]} tick={false}/><Radar name="Твой профиль" dataKey="student" stroke="#e76443" fill="#e76443" fillOpacity={.3}/><Radar name="Средний поступивший" dataKey="admitted" stroke="#17352e" fill="#17352e" fillOpacity={.12}/><Tooltip/></RadarChart></ResponsiveContainer></div></div><div className="panel p-6"><h3 className="font-extrabold text-xl">Coherence / Spike</h3><p className="mt-4 leading-relaxed">{evaluation.coherence_notes}</p><p className="notice mt-4">{evaluation.spike_alignment_notes}</p><h4 className="font-extrabold mt-6">Следующие усиления</h4><ul className="space-y-3 mt-3">{evaluation.suggestions?.map(x=><li className="reason" key={x}><Check size={16}/>{x}</li>)}</ul></div></div>}</section>}

function Assistant(){const {user}=useAuth();const {profile}=useWorkspace();const [messages,setMessages]=useState([]);const [input,setInput]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState('');useEffect(()=>{supabase.from('ai_chat_messages').select('id,role,content,created_at').eq('user_id',user.id).order('created_at').then(({data,error:loadError})=>{if(loadError)setError('Не удалось загрузить историю чата.');else setMessages(data||[])})},[user.id]);async function send(e){e.preventDefault();const message=input.trim();if(!message||busy)return;setInput('');setError('');setBusy(true);const userMessage={id:`local-user-${Date.now()}`,role:'user',content:message};const assistantId=`local-assistant-${Date.now()}`;setMessages(current=>[...current,userMessage,{id:assistantId,role:'assistant',content:''}]);try{const {data:{session}}=await supabase.auth.getSession();const response=await fetch(`${import.meta.env.VITE_SUPABASE_URL}/functions/v1/assistant-chat`,{method:'POST',headers:{Authorization:`Bearer ${session?.access_token}`,'Content-Type':'application/json',apikey:import.meta.env.VITE_SUPABASE_ANON_KEY},body:JSON.stringify({message})});if(!response.ok){const payload=await response.json().catch(()=>({}));throw new Error(payload.error||'Ассистент временно недоступен.')}if(!response.body)throw new Error('Браузер не поддерживает потоковый ответ.');const reader=response.body.getReader();const decoder=new TextDecoder();while(true){const {done,value}=await reader.read();if(done)break;const chunk=decoder.decode(value,{stream:true});setMessages(current=>current.map(item=>item.id===assistantId?{...item,content:item.content+chunk}:item))}}catch(sendError){setMessages(current=>current.filter(item=>item.id!==assistantId));setError(sendError.message)}finally{setBusy(false)}}return <section><PageTitle icon={<MessageCircle/>} eyebrow="AI-ассистент" title="Эссе и стратегия — с контекстом твоего профиля." sub={`Ассистент видит сохранённый профиль, шортлист и roadmap под ${profile.target_major||'твоё направление'}, но не гарантирует поступление и не выдумывает достижения.`}/><div className="panel mt-8 overflow-hidden"><div className="h-[55vh] min-h-[360px] overflow-y-auto p-5 md:p-7 space-y-4" aria-live="polite">{!messages.length&&!busy&&<div className="empty-state"><Sparkles size={34}/><h3 className="font-extrabold mt-3">С чего начнём?</h3><p className="muted max-w-lg">Пришли черновик эссе для разбора или спроси, как усилить стратегию под выбранные вузы.</p></div>}{messages.map(item=><div key={item.id} className={item.role==='user'?'ml-auto max-w-[85%] rounded-2xl bg-[#17352e] text-white p-4':'mr-auto max-w-[90%] rounded-2xl bg-[#f2e6df] p-4'}><div className="text-xs font-bold uppercase tracking-wider opacity-60 mb-1">{item.role==='user'?'Ты':'Northstar AI'}</div><p className="whitespace-pre-wrap leading-relaxed">{item.content||<span className="inline-flex gap-1"><span className="animate-pulse">●</span><span className="animate-pulse">●</span><span className="animate-pulse">●</span></span>}</p></div>)}</div><form onSubmit={send} className="border-t border-black/10 p-4 flex gap-3"><textarea rows="2" maxLength="12000" value={input} onChange={e=>setInput(e.target.value)} placeholder="Вставь абзац эссе или задай вопрос о стратегии…" aria-label="Сообщение AI-ассистенту"/><button className="btn-primary self-end" disabled={busy||!input.trim()}><Send size={16}/><span className="hidden sm:inline">Отправить</span></button></form></div>{error&&<p className="error mt-4">{error}</p>}<p className="source mt-3">История хранится в защищённой базе и удаляется вместе с аккаунтом.</p></section>}

function Roadmap(){const {profile}=useWorkspace();const [events,setEvents]=useState([]);const [busy,setBusy]=useState(false);const [message,setMessage]=useState('');const load=()=>supabase.from('calendar_events').select('*').eq('profile_id',profile.id).order('date').then(({data})=>setEvents(data||[]));useEffect(load,[profile.id]);async function generate(){setBusy(true);setMessage('');if(profile.needs_full_aid)await supabase.rpc('add_financial_aid_deadlines',{target_profile_id:profile.id});const {data:shortlist,error:shortlistError}=await supabase.from('shortlist').select('university_id,universities(name,country,region,other_requirements)').eq('profile_id',profile.id).order('created_at');if(shortlistError){setMessage(shortlistError.message);setBusy(false);return}const today=new Date();today.setHours(12,0,0,0);const cycleStart=new Date(today.getFullYear()+(today.getMonth()>=7?1:0),7,1,12);const iso=date=>date.toISOString().slice(0,10);const boundedDate=days=>{const candidate=new Date(today);candidate.setDate(candidate.getDate()+days);return candidate>cycleStart?new Date(cycleStart):candidate};const auto=[];for(const [index,row] of (shortlist||[]).entries()){const university=row.universities;if(!university)continue;const rawTests=university.other_requirements?.tests;const tests=Array.isArray(rawTests)?rawTests.join(', '):rawTests||'уточнить тесты и пакет документов';auto.push({profile_id:profile.id,title:`Подготовить требования — ${university.name}`,date:iso(boundedDate(21+index*14)),type:'task',description:`Автоматический этап под ${profile.target_major}. Для ${university.name}: ${tests}.`,source:'auto_ai_roadmap'});const region=`${university.country||''} ${university.region||''}`.toLowerCase();if(region.includes('hong kong')){const interviewDate=new Date(cycleStart);interviewDate.setDate(interviewDate.getDate()-42);auto.push({profile_id:profile.id,title:`Подготовка к интервью — ${university.name}`,date:iso(interviewDate<today?boundedDate(14):interviewDate),type:'task',description:`Автоматический этап под ${profile.target_major}. Провести mock-интервью примерно за 6 недель до цикла подачи в ${university.name}.`,source:'auto_ai_roadmap'})}}const targets=(profile.target_countries||[]).map(x=>x.toLowerCase());if(targets.some(x=>x.includes('kazakhstan'))&&!profile.nuet){auto.push({profile_id:profile.id,title:'Сдать/пересдать NUET',date:iso(boundedDate(35)),type:'exam',description:`Автоматический этап под ${profile.target_major}. Зафиксировать диагностический результат NUET и план пересдачи.`,source:'auto_ai_roadmap'})}if(auto.length){const {error}=await supabase.from('calendar_events').upsert(auto,{onConflict:'profile_id,title,date,source'});if(error)setMessage(error.message);else setMessage(`Добавлено этапов: ${auto.length}.`)}else setMessage('Добавь вузы в шортлист — roadmap будет построен под их требования.');await load();setBusy(false)}async function addManual(){await supabase.from('calendar_events').insert({profile_id:profile.id,title:'Новая задача',date:new Date().toISOString().slice(0,10),type:'task',description:'Добавлено вручную',source:'manual'});load()}return <section><PageTitle icon={<CalendarDays/>} eyebrow="roadmap + calendar" title="Дедлайны живут в базе, не во вкладке." sub="Northstar различает ручные события, AI-roadmap и дедлайны финансовой помощи."/><div className="flex flex-wrap gap-3 mt-7"><button className="btn-primary" disabled={busy} onClick={generate}><Sparkles size={16}/>{busy?'Генерируем…':'Сгенерировать roadmap'}</button><button className="btn-ghost" onClick={addManual}><Plus size={16}/>Добавить вручную</button></div>{message&&<p className={message.startsWith('Добав')?'notice mt-4':'source mt-4'}>{message}</p>}<div className="timeline mt-8">{events.map((e,i)=><div className="timeline-item" key={e.id}><div className="timeline-dot">{i+1}</div><div className="panel p-5 flex-1"><div className="flex flex-wrap justify-between gap-3"><div><div className="text-xs font-bold uppercase tracking-wider text-[#e76443]">{e.date} · {e.type}</div><h3 className="font-extrabold text-lg mt-1">{e.title}</h3><p className="muted mt-1">{e.description}</p></div><span className="badge bg-[#f2e6df]">{e.source}</span></div><button className="btn-ghost mt-3" onClick={()=>downloadIcs(e)}><CalendarDays size={15}/>Экспорт .ics</button></div></div>)}</div>{!events.length&&<div className="empty-state panel mt-8"><CalendarDays size={38}/><h3 className="font-extrabold mt-3">Календарь пока пуст</h3><p className="muted">Сгенерируй roadmap — aid-дедлайны добавятся автоматически, если включена полная помощь.</p></div>}</section>}

function Networking(){const {profile,universities}=useWorkspace();const [kind,setKind]=useState('research');const [university,setUniversity]=useState(universities[0]?.name||'университета');const generated=useMemo(()=>kind==='research'?`Subject: Prospective student interested in ${profile.target_major}\n\nDear Professor,\n\nMy name is ${profile.full_name||'[Name]'}, and I am a student at ${profile.school||'[School]'} exploring ${profile.target_major}. I am particularly interested in your work at ${university}. My current portfolio focuses on ${profile.narrative_about_me||'[briefly describe your strongest project]'} and I would be grateful to learn whether there may be a small, well-scoped way to contribute to your research.\n\nI can share a concise portfolio and am happy to complete a trial task.\n\nBest regards,\n${profile.full_name||'[Name]'}`:`Subject: Financial aid reconsideration request\n\nDear Financial Aid Office,\n\nThank you for reviewing my application to ${university}. I am writing to respectfully ask whether my financial aid package can be reconsidered. My family’s realistic annual contribution is ${profile.currency} ${Number(profile.annual_budget||0).toLocaleString()}, and I require substantial support to enroll.\n\nI can provide updated financial documents and context upon request. ${profile.narrative_about_me||''}\n\nSincerely,\n${profile.full_name||'[Name]'}`,[kind,university,profile]);const [draft,setDraft]=useState(generated);useEffect(()=>setDraft(generated),[generated]);return <section><PageTitle icon={<Mail/>} eyebrow="networking utility" title="Черновик, который звучит как ты." sub="Northstar использует профиль для первого варианта. Перед отправкой обязательно проверь факты и адресата."/><div className="grid md:grid-cols-[.7fr_1.3fr] gap-5 mt-8"><div className="panel p-6"><Field label="Тип письма"><select value={kind} onChange={e=>setKind(e.target.value)}><option value="research">Research inquiry</option><option value="aid">Financial aid appeal</option></select></Field><Field label="Университет"><select value={university} onChange={e=>setUniversity(e.target.value)}>{universities.map(u=><option key={u.id}>{u.name}</option>)}</select></Field><div className="notice mt-5">Не отправляй массово. Добавь конкретную работу профессора или детали aid package.</div></div><div className="panel p-6"><textarea className="email-editor" value={draft} onChange={e=>setDraft(e.target.value)}/><button className="btn-primary mt-4" onClick={()=>navigator.clipboard.writeText(draft)}><Clipboard size={16}/>Скопировать черновик</button></div></div></section>}

function Settings(){const {signOut}=useAuth();const navigate=useNavigate();const [confirmOpen,setConfirmOpen]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function deleteAccount(){setBusy(true);setError('');const {error:invokeError}=await supabase.functions.invoke('delete-account');if(invokeError){let message=invokeError.message;try{const payload=await invokeError.context?.json();message=payload?.error||message}catch{/* keep SDK error */}setError(message);setBusy(false);return}await signOut();navigate('/',{replace:true})}return <section><PageTitle icon={<SettingsIcon/>} eyebrow="настройки аккаунта" title="Ты контролируешь свои данные." sub="Удаление аккаунта необратимо: профиль, активности, оценки, roadmap и шортлист удалятся каскадно."/><div className="panel p-6 mt-8 border border-red-200"><h2 className="font-extrabold text-xl">Опасная зона</h2><p className="muted mt-2">Это действие удалит запись Supabase Auth и все связанные данные.</p><button className="btn-ghost mt-5 text-red-700" onClick={()=>setConfirmOpen(true)}><Trash2 size={16}/>Удалить аккаунт</button>{error&&<p className="error mt-4">{error}</p>}</div>{confirmOpen&&<div className="fixed inset-0 z-50 bg-black/60 grid place-items-center p-5" role="dialog" aria-modal="true" aria-labelledby="delete-account-title"><div className="panel p-7 w-full max-w-md"><div className="flex justify-between gap-4"><h2 id="delete-account-title" className="display text-3xl">Точно удалить аккаунт?</h2><button className="icon-button" aria-label="Закрыть подтверждение" disabled={busy} onClick={()=>setConfirmOpen(false)}><X size={18}/></button></div><p className="muted mt-4">Вернуть профиль, AI-оценки, события календаря и шортлист после удаления будет невозможно.</p><div className="flex flex-wrap justify-end gap-3 mt-6"><button className="btn-ghost" disabled={busy} onClick={()=>setConfirmOpen(false)}>Отмена</button><button className="btn-primary bg-red-700" disabled={busy} onClick={deleteAccount}><Trash2 size={16}/>{busy?'Удаляем…':'Да, удалить навсегда'}</button></div></div></div>}</section>}

function PageTitle({icon,eyebrow,title,sub}){return <div><div className="eyebrow">{icon}<span>{eyebrow}</span></div><h1 className="display text-5xl mt-4">{title}</h1><p className="muted mt-3 max-w-3xl">{sub}</p></div>}
function Field({label,children}){return <label className="block mt-5"><span className="label">{label}</span><div className="mt-2">{children}</div></label>}
function downloadIcs(e){const start=e.date.replaceAll('-','');const content=['BEGIN:VCALENDAR','VERSION:2.0','PRODID:-//Northstar//Calendar//EN','BEGIN:VEVENT',`UID:${e.id}@northstar`,`DTSTART;VALUE=DATE:${start}`,`SUMMARY:${e.title}`,`DESCRIPTION:${e.description||''}`,'END:VEVENT','END:VCALENDAR'].join('\r\n');const url=URL.createObjectURL(new Blob([content],{type:'text/calendar'}));const a=document.createElement('a');a.href=url;a.download='northstar-event.ics';a.click();URL.revokeObjectURL(url)}

export default function App(){return <AuthProvider><BrowserRouter><Routes><Route path="/" element={<Landing/>}/><Route path="/login" element={<Login/>}/><Route path="/reset-password" element={<ResetPassword/>}/><Route element={<ProtectedRoute><Shell/></ProtectedRoute>}><Route path="/portfolio" element={<Portfolio/>}/><Route path="/evaluator" element={<Evaluator/>}/><Route path="/assistant" element={<Assistant/>}/><Route path="/universities" element={<Universities/>}/><Route path="/compare" element={<Compare/>}/><Route path="/roadmap" element={<Roadmap/>}/><Route path="/networking" element={<Networking/>}/><Route path="/settings" element={<Settings/>}/></Route><Route path="*" element={<Navigate to="/" replace/>}/></Routes></BrowserRouter></AuthProvider>}
